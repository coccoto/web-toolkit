export default async () => {
    if (false) {
        return 'http://localhost:18010'

    } else if (true) {
        return 'https://webtoolkit'

    } else {
        console.error('error')
        return null
    }
}